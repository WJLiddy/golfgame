using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AI
{
    static Vector2 returnBestPathToTarget(Vector2 origin, Vector2 target)
    {
        // Begin by seeing if we can reach the target directly.
        return Vector2.zero;
    }
}
